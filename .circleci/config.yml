version: 2.1
jobs:
  Build-and-Run:
    docker:
      - image: cypress/browsers
    steps:
      - checkout
      - run: 
        name: "Percy Snapshot Token"
        command: |
          echo $PERCY_TOKEN 
      - run: npm install
      - run: npm run percy:ci
workflows:
  version: 2.1
  Portfolio-Tests:
    jobs:
      - Build-and-Run