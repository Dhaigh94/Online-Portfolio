version: 2.1
orbs:
  node: circleci/node@1.1.6
  cypress: cypress-io/cypress@1.16.1
  workflows:
    build:
      jobs:
        - cypress/run:
          start: npm start
          wait-on: 'http://localhost:4200'
workflows:
  Portfolio-Tests:
      jobs:
        - cypress/run